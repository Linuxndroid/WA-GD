[auth]
gmail = YourEmail

# The account app password ou plain text password
# You will be prompted if omitted.
password = password

# The result of "adb shell settings get secure android_id".
android_id = android_id